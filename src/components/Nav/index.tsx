import Wallet from "../wallet";

export default function Nav() {
    return (
        <nav className="py-8 w-5/6 flex items-center justify-end mx-auto">
            <Wallet />
        </nav>
    );
}
